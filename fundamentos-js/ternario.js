const resultado = nota => nota >=7 ? 'aprovado' : 'reprovado' //Expressão relacional, primeiro se nota for verdadeiro (>=7) retorna aprovado, se não for retorna reprovado. (tipo um If / else)

console.log(resultado (7.1));
console.log(resultado (6.9));